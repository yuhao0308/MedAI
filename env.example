# Modal Labs Cloud GPU Inference
# Get these from: https://modal.com/settings (after running 'modal token new')
# Or find them in ~/.modal.toml after authentication
MODAL_TOKEN_ID=your_token_id_here
MODAL_TOKEN_SECRET=your_token_secret_here

# Railway automatically sets PORT - no need to configure
# PORT=8501

# ==========================================
# SETUP INSTRUCTIONS:
# ==========================================
# 1. Install Modal locally: pip install modal
# 2. Authenticate: modal token new
# 3. Deploy the inference app: modal deploy src/cloud/modal_inference.py
# 4. Upload your model (happens automatically on first inference)
# 5. Get your token from ~/.modal.toml or Modal dashboard
# 6. Add MODAL_TOKEN_ID and MODAL_TOKEN_SECRET to Railway environment variables
# ==========================================
